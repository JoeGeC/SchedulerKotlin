This program can be run on the command line using

cat input.txt | java -jar SchedulerParser.jar <simulated current time in HH:MM>

The .jar file and input.txt files can be found in the project's root directory.